export const QUIZ_OPTION_COUNT = 4;
export const MAX_PRACTICE_LEARNING_ITEMS = 2;
export const QUIZ_FALLBACK_OPTIONS = [
  "The relationship is fixed and cannot adapt when its inputs change.",
  "Each component operates independently without affecting the others.",
];
